apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-roomservice
  labels:
    app: {{ .Chart.Name }}-roomservice
spec:
  selector:
    app: {{ .Chart.Name }}-roomservice
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP

apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-authservice
  labels:
    app: {{ .Chart.Name }}-authservice
spec:
  selector:
    app: {{ .Chart.Name }}-authservice
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
